# Gitleaks подхватывает этот файл из корня репозитория автоматически.
# Стандартные правила; ниже — точечные allowlist'ы для ложных срабатываний.

title = "OMNIX gitleaks config"

[allowlist]
description = "Заведомо фиктивный токен в unit-тесте (следующим коммитом литерал заменён программной сборкой)."
fingerprints = [
    "32b17daaa428419510efcc6b229b8544dd1dd38f:app/src/test/java/com/omnix/assistant/core/license/HttpLicenseServerValidatorTest.kt:generic-api-key:72",
]
